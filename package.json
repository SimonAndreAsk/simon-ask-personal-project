{
  "name": "simonask.io",
  "private": true,
  "description": "Repo-root tooling (tracking contract validation)",
  "scripts": {
    "validate:tracking": "node scripts/validate-datalayer.mjs"
  },
  "devDependencies": {
    "ajv": "^8.17.1",
    "ajv-formats": "^3.0.1"
  },
  "engines": {
    "node": ">=20"
  }
}
