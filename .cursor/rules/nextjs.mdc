---
description: Next.js app conventions for simonask.io
globs: nextjs-simonask.io/**/*
alwaysApply: false
---

# Next.js (`nextjs-simonask.io`)

Next **16** — read `node_modules/next/dist/docs/` before APIs from training data. Tailwind 4 · `@/` → `src/`.

- Data: `sanityFetch()` in `src/sanity/load.ts`; GROQ in `src/sanity/queries.ts`
- Drafts on staging: `NEXT_PUBLIC_SITE_ENV=staging` + `SANITY_API_READ_TOKEN`
- Detail: `memories/nextjs-conventions.md`
