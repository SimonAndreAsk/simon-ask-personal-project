# Copy to .env for local use only (never commit .env)
#
# projectId and dataset are already set in sanity.config.ts.
# Add variables here only if you use Sanity CLI deploy or private tokens.

# SANITY_AUTH_TOKEN=
# SANITY_STUDIO_API_DATASET=production
