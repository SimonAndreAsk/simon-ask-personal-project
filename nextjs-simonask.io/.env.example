# Copy to .env.local for local overrides (never commit .env.local)
#
# The site reads published Sanity content without a token by default.
# Only add these if you enable draft preview, webhooks, or private datasets.

# SANITY_API_READ_TOKEN=
# SANITY_REVALIDATE_SECRET=
